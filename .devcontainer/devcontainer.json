{
  "name": "poker-clock",
  "image": "mcr.microsoft.com/devcontainers/python:3.12",
  "postCreateCommand": "cd server && python -m venv .venv && .venv/bin/pip install -r requirements.txt && .venv/bin/python manage.py migrate && cd ../client-react && npm install",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "dbaeumer.vscode-eslint"
      ]
    }
  }
}
