http:
  routers:
    pokerklokke:
      # Path-basert routing (ingen stripPrefix). Endre basepath hvis du vil.
      rule: "PathPrefix(`/pokerklokke`)"
      entryPoints:
        - websecure
      tls:
        certResolver: le
      service: pokerklokke-svc
      priority: 10

  services:
    pokerklokke-svc:
      loadBalancer:
        servers:
          - url: "http://127.0.0.1:8000"
